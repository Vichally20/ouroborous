import 'package:flutter/material.dart';

class VitruvianColors {
  /// Void Black (`#050505`) - Main diegetic manuscript background
  static const Color voidBlack = Color(0xFF050505);

  /// Aged Bone / Vellum (`#D4CFC7`) - Primary text and schematic lines
  static const Color agedBone = Color(0xFFD4CFC7);

  /// Sepia / Burnt Umber (`#8B7355`) - Active states, highlighted slots, borders
  static const Color sepiaUmber = Color(0xFF8B7355);

  /// Rust / Dried Blood (`#662222`) - Danger, depleted sanity, critical feedback
  static const Color rustBlood = Color(0xFF662222);

  /// Lighter Vellum - Used for tactical navigation map background [Screen 4.5]
  static const Color lighterVellum = Color(0xFFE3DFD8);

  /// Subtle Etched Border color (1px hairline)
  static const Color etchedHairline = Color(0xFF2A2825);

  /// Gold Leaf (`#C0A070`) - Loot callouts and precious relics
  static const Color goldLeaf = Color(0xFFC0A070);
}
